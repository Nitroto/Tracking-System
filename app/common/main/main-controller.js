'use strict';

angular.module('trackingSystem.common', [])
    .controller('MainController', [
        '$scope',
        '$http',
        'identity',
        function ($scope, $http, identity) {
        }]);